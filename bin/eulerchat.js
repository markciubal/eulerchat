#!/usr/bin/env node
/**
 * eulerchat [--interests N] [--people N] [--port N] [--moderator FINGERPRINT]...
 *
 * With no flags, serves the small hand-written world — three subjects, which
 * is the one size every part of this draws perfectly. `--interests 1000`
 * builds a synthetic population instead.
 */
import '../server/index.js';
